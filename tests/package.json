{
  "name": "parchehban-tests",
  "version": "9.2.0",
  "private": true,
  "description": "تست‌های خودکار پارچه‌بان",
  "scripts": {
    "test": "node run-all.js",
    "test:all": "node run-all.js --all",
    "lint": "cat ../js/*.js > /tmp/pb-all.js && eslint --no-eslintrc -c eslint.json /tmp/pb-all.js"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "fake-indexeddb": "^5.0.2",
    "jsdom": "^24.0.0",
    "playwright-core": "^1.47.0"
  }
}

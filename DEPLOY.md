# راهنمای بارگذاری پارچه‌بان روی GitHub و Vercel

این راهنما فرض می‌کند هیچ حسابی ندارید و از صفر شروع می‌کنید. کل کار حدود ۲۰ دقیقه است و **رایگان** انجام می‌شود.

> **چرا اصلاً روی اینترنت بگذاریم؟** سه دلیل: از هر دستگاهی (گوشی، لپ‌تاپ) در دسترس است؛ دکمه «نصب برنامه» فقط روی **https** کار می‌کند و روی `http://localhost` یا آی‌پی محلی فعال نمی‌شود؛ و به‌روزرسانی نسخه‌های بعدی با یک دستور انجام می‌شود.

---

## ⚠️ قبل از هر کاری: پشتیبان بگیرید

اگر تا حالا با نسخه‌های قبلی کار کرده‌اید و اطلاعات واقعی داخلش دارید:

1. برنامه را باز کنید → **تنظیمات → بکاپ → ذخیره**
2. فایل `parchehban-backup-....json` را جای امنی نگه دارید.

**دلیل مهم:** اصلاح علامت مانده حساب (بند ۴.۲) یک **مهاجرت یک‌بارمصرف** است که در اولین اجرای نسخه جدید روی داده‌ها اعمال می‌شود. اگر چیزی غیرمنتظره دیدید، بدون پشتیبان راه بازگشتی نیست.

نکته: داده‌های شما روی مرورگر خودتان است، نه در فایل‌های برنامه. بارگذاری روی Vercel داده‌ها را جابه‌جا **نمی‌کند** — روی دستگاه جدید باید از همان فایل پشتیبان «بازیابی» بگیرید.

---

## گام ۱ — ساخت حساب GitHub

1. به [github.com/signup](https://github.com/signup) بروید.
2. ایمیل، رمز و یک نام کاربری (مثلاً `mnasaji`) وارد کنید.
3. کد تأییدی که به ایمیلتان می‌آید را بزنید.
4. در سؤال‌های ابتدایی، پلن **Free** را انتخاب کنید.

---

## گام ۲ — ساخت مخزن (Repository)

1. بالا سمت راست، روی **+** → **New repository** بزنید.
2. تنظیمات:
   - **Repository name:** `parcheban`
   - **Description:** (اختیاری) `حسابداری و انبارداری پارچه`
   - **Private** را انتخاب کنید — این برنامه شخصی است، دلیلی ندارد عمومی باشد.
   - تیک `Add a README file` را **نزنید** (خودمان README داریم).
3. **Create repository**.

> Vercel با مخزن Private هم بدون مشکل کار می‌کند؛ نیازی به عمومی کردن نیست.

---

## گام ۳ — بالا بردن فایل‌ها

دو راه دارید. راه اول ساده‌تر است و برای شروع کافی است.

### راه الف — کشیدن و رها کردن در مرورگر (ساده)

1. فایل `parcheban.zip` را روی سیستم خودتان **از حالت فشرده خارج** کنید. یک پوشه به نام `parcheban` می‌سازد.
2. در صفحه مخزن خالی، روی لینک **uploading an existing file** بزنید.
3. **محتویات داخل** پوشه `parcheban` را انتخاب کنید و بکشید و در صفحه رها کنید — نه خود پوشه را.

   یعنی این‌ها باید در ریشه مخزن باشند: `index.html`، `sw.js`، `manifest.json`، و پوشه‌های `js/`، `css/`، `vendor/`، `icons/`، `tests/`، `supabase/`.

   > اگر خود پوشه را بکشید، آدرس سایت `.../parcheban/index.html` می‌شود و برنامه بالا نمی‌آید. `index.html` **باید** در ریشه باشد.

4. پایین صفحه در کادر Commit message بنویسید `نسخه ۹.۴` و **Commit changes** را بزنید.

> مرورگر برای تعداد زیاد فایل ممکن است کند باشد. اگر پوشه `vendor/fonts` بالا نرفت، جداگانه آپلود کنید (Add file → Upload files، مسیر را در کادر بالای صفحه تایپ کنید).

### راه ب — با Git از خط فرمان (برای به‌روزرسانی‌های بعدی بهتر است)

اگر Git نصب نیست: [git-scm.com/downloads](https://git-scm.com/downloads).

```bash
cd مسیر/پوشه/parcheban

git init
git add .
git commit -m "نسخه ۹.۴"
git branch -M main
git remote add origin https://github.com/USERNAME/parcheban.git
git push -u origin main
```

`USERNAME` را با نام کاربری خودتان عوض کنید. اولین بار مرورگر برای ورود باز می‌شود.

نسخه‌های بعدی فقط سه دستور:

```bash
git add .
git commit -m "نسخه ۹.۵"
git push
```

---

## گام ۴ — اتصال Vercel

1. به [vercel.com/signup](https://vercel.com/signup) بروید.
2. **Continue with GitHub** را بزنید (حساب جدا نسازید — با همان GitHub وارد شوید).
3. پلن **Hobby** را انتخاب کنید (رایگان). اگر پرسید شخصی است یا کاری، **Personal** را بزنید.
4. به Vercel اجازه دسترسی به مخزن بدهید. توصیه: **Only select repositories** → فقط `parcheban` را انتخاب کنید.
5. در داشبورد → **Add New… → Project** → کنار `parcheban` روی **Import** بزنید.
6. در صفحه تنظیمات **هیچ‌چیز را عوض نکنید**:
   - Framework Preset: `Other`
   - Root Directory: خالی (`./`)
   - Build Command: خالی
   - Output Directory: خالی

   > این برنامه HTML/CSS/JS ساده است و **مرحله Build ندارد**. اگر Vercel چیزی مثل Next.js تشخیص داد، دستی روی `Other` بگذارید، وگرنه خطای build می‌گیرید.
7. **Deploy** را بزنید. حدود ۳۰ ثانیه بعد آدرسی مثل `https://parcheban.vercel.app` می‌گیرید.

از این به بعد هر بار که به GitHub push کنید (یا در مرورگر فایلی را Commit کنید)، Vercel خودکار نسخه جدید را منتشر می‌کند.

---

## گام ۵ — اولین ورود و بررسی

1. آدرس Vercel را باز کنید. توجه کنید که با **https** شروع می‌شود.
2. با `admin` / `admin123` وارد شوید.
3. **بلافاصله رمز را عوض کنید:** تنظیمات → کاربران. سایت روی اینترنت است و رمز پیش‌فرض را همه می‌دانند.
4. اگر پشتیبانی از قبل داشتید: تنظیمات → بکاپ → **بازیابی** و فایل JSON را بدهید.
5. بعد از ورود، نوار «پشتیبان بگیرید» را که دیدید یک بار پشتیبان بگیرید تا مبنای یادآوری‌های بعدی ثبت شود.

### نصب روی گوشی و دسکتاپ

- **اندروید/کروم:** منوی مرورگر → «Add to Home screen» یا دکمه «نصب برنامه» داخل خود برنامه.
- **آیفون/سافاری:** دکمه Share → «Add to Home Screen».
- **ویندوز/کروم:** آیکون نصب در نوار آدرس، یا دکمه «نصب برنامه» در برنامه.

بعد از نصب، برنامه **بدون اینترنت** هم باز می‌شود.

---

## اگر نسخه قدیمی را دیدید (مهم)

برنامه Service Worker دارد و فایل‌ها را داخل مرورگر ذخیره می‌کند. بعد از انتشار نسخه جدید ممکن است چند دقیقه نسخه قبلی را ببینید. اگر گوشه پایین منو `v9.4` را نشان نداد:

**راه سریع:** `Ctrl+Shift+R` (مک: `Cmd+Shift+R`).

**راه قطعی:**

1. `F12` → تب **Application**
2. **Service Workers** → روی نسخه فعلی **Unregister**
3. **Storage** → **Clear site data** — ⚠️ تیک **IndexedDB را بردارید**، وگرنه همه دفاترتان پاک می‌شود. فقط Cache Storage را پاک کنید.
4. صفحه را Refresh کنید.

---

## اجرای تست‌ها (اختیاری)

اگر خواستید مطمئن شوید همه‌چیز سالم است، روی سیستم خودتان:

```bash
cd parcheban/tests
npm install
cd ..
python3 -m http.server 8000
```

در یک پنجره دیگر:

```bash
cd parcheban/tests
PB_PORT=8000 node run-all.js --all
```

باید ۱۳ دسته تست را «قبول» نشان بدهد. اگر `--all` را نزنید فقط تست‌های منطقی (بدون مرورگر) اجرا می‌شوند.

---

## چند نکته درباره امنیت

- مخزن را **Private** نگه دارید.
- آدرس `parcheban.vercel.app` قابل حدس زدن است و صفحه ورود روی اینترنت باز است. رمز قوی بگذارید.
- اگر خواستید آدرس در دسترس عموم نباشد، Vercel در پلن رایگان گزینه **Password Protection** برای کل سایت **ندارد** (فقط در پلن Pro). فعلاً رمز قوی روی حساب کاربری، کافی‌ترین راه است.
- فایل پشتیبان JSON **رمزنگاری نشده** است و تمام اطلاعات مالی‌تان در آن است. در فضای ابری عمومی نگذارید.

---

## چند نکته درباره پشتیبان

یادآور خودکار کمک می‌کند فراموش نکنید، ولی خودش پشتیبان نمی‌گیرد — باید دکمه را بزنید و فایل ذخیره شود.

پیشنهاد: فایل‌های پشتیبان را با تاریخشان در یک پوشه نگه دارید و چند نسخه آخر را حذف نکنید. اگر خطایی در داده‌ها دیر کشف شود، پشتیبان یک‌ماه پیش نجات‌دهنده است.

---

## قدم بعدی: چند دستگاه با اطلاعات مشترک

الان اطلاعات هر دستگاه **جدا** است؛ گوشی و لپ‌تاپ دفاتر مشترک ندارند. برای مشترک شدن، به یک پایگاه داده روی سرور نیاز است. طرح اولیه‌اش در `supabase/schema.sql` و بخش «مسیر انتقال به سرور» فایل `CHANGELOG.md` آمده. هر وقت خواستید، این را جداگانه پیش می‌بریم.

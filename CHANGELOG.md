# فهرست تغییرات

## نسخه ۹.۴

### جست‌وجوی تایپی کالا در فاکتور
ردیف‌های فاکتور دیگر از فهرست بازشو (`select`) استفاده نمی‌کنند. حالا در ستون «کالا» چند حرف تایپ می‌کنید و فهرست فیلتر می‌شود:

- جست‌وجو روی **نام کالا، کد کالیته و شید رنگ** انجام می‌شود.
- «ی» و «ک» عربی با فارسی یکی گرفته می‌شود، ارقام فارسی هم پذیرفته می‌شود؛ پس «حرير» هم «حریر» را پیدا می‌کند.
- کالاهایی که نامشان با عبارت تایپ‌شده **شروع** می‌شود اول فهرست می‌آیند.
- موجودی هر کالا کنار نامش نمایش داده می‌شود (سبز = موجود، قرمز = ناموجود/کسری). این عدد فقط برای اطلاع شماست و وارد فاکتور نمی‌شود.
- با ↑ ↓ حرکت، با Enter انتخاب، با Esc بستن. بعد از انتخاب، مکان‌نما خودکار به فیلد «تعداد» می‌رود.
- اگر متنی تایپ شده باشد ولی کالایی انتخاب نشده باشد، کادر زرد علامت می‌خورد تا ردیف با نام نیمه‌تایپ‌شده ثبت نشود. اگر فقط یک نتیجه داشته باشد، همان خودکار انتخاب می‌شود.

پر شدن خودکار کالیته و شید، محاسبه جمع و کنترل فروش بیش از موجودی همه بدون تغییر کار می‌کنند.

### یادآور پشتیبان‌گیری
تمام اطلاعات برنامه فقط در IndexedDB همان مرورگر ذخیره می‌شود. پاک کردن داده‌های مرورگر یا خرابی پروفایل به معنی از دست رفتن کل دفاتر است. برای همین نوار یادآوری اضافه شد که پس از ورود بررسی می‌کند:

- اگر **هیچ‌وقت** پشتیبان نگرفته باشید و سندی ثبت شده باشد.
- اگر **۷ روز** از آخرین پشتیبان گذشته باشد و دفاتر تغییر کرده باشد.
- یا اگر از آخرین پشتیبان **۲۰ سند** تازه ثبت شده باشد.

نوار دلیل را دقیق می‌گوید (مثلاً «۸ روز از آخرین پشتیبان گذشته») و تاریخ آخرین پشتیبان را نشان می‌دهد. با دکمه «پشتیبان بگیر» فایل ذخیره و زمان ثبت می‌شود. دکمه «بعداً» فقط تا پایان همان نشست ساکت می‌کند، نه برای همیشه.

### ارتقای رمز قدیمی — بررسی و تأیید شد
در بازبینی مشخص شد این قابلیت **از قبل پیاده شده بود** و درست کار می‌کند: اگر رمز کاربری با روش قدیمی (`btoa`) ذخیره شده باشد، در اولین ورود موفق خودکار به SHA-256 تبدیل و ذخیره می‌شود. یادداشت نسخه قبلی که این مورد را «انجام‌نشده» علامت زده بود اشتباه بود. حالا تست `tests/auth.js` این رفتار را از ابتدا تا انتها تأیید می‌کند.

### رفع اشکال — پیام گیج‌کننده هنگام ورود با http
`crypto.subtle` که برای رمزنگاری استفاده می‌شود فقط در «بستر امن» (https یا localhost) وجود دارد. اگر برنامه را با http روی آی‌پی شبکه باز می‌کردید (مثلاً `http://192.168.1.5:8000` از گوشی)، ورود با پیام نامفهوم `Cannot read properties of undefined` شکست می‌خورد و دلیلش مشخص نبود. حالا پیام روشن فارسی نمایش داده می‌شود که برنامه باید با https یا localhost باز شود.

### رفع اشکال — خالی کردن کادر کالا
در نسخه اول کادر جست‌وجو، اگر کالایی انتخاب و بعد کادر خالی می‌شد، کالای قبلی همچنان به ردیف چسبیده بود و فاکتور با کالای اشتباه ثبت می‌شد. علت این بود که ملاک تشخیص، نشانگر روی خود کادر بود که به‌محض تایپ پاک می‌شد. حالا مبنا خودِ ردیف فاکتور است.

### رفع اشکال — یادآور با حذف سند
شرط یادآوری زمانی فقط «افزایش» تعداد اسناد را می‌دید. اگر سندی حذف کرده بودید تعداد کم می‌شد و با اینکه دفاتر عوض شده بود هیچ یادآوری نمی‌آمد. حالا هر نوع تغییری در نظر گرفته می‌شود.

### رفع اشکال — بریده شدن فهرست بازشو
فهرست جست‌وجو ابتدا داخل ردیف جدول قرار داشت و کارت «اقلام» (`overflow:hidden`) و `backdrop-filter` پنجره فاکتور آن را می‌بریدند؛ فقط نوار باریکی از فهرست دیده می‌شد. حالا فهرست مستقیم به `body` چسبانده می‌شود و مختصاتش نسبت به کادر حساب می‌شود — اگر پایین صفحه جا نباشد به سمت بالا باز می‌شود.

### تست‌ها
سه دسته تست تازه اضافه شد و مجموع به **۱۳ دسته** رسید:

- `tests/prodpick.js` — جست‌وجوی تایپی کالا (۱۷ بررسی)
- `tests/auth.js` — ورود و ارتقای خودکار رمز (۶ بررسی)
- `tests/backup.js` — یادآور پشتیبان‌گیری (۱۵ بررسی)

## نسخه ۹.۳

### بازطراحی فرم‌ها
ماژول تازه `js/05b-form.js` اضافه شد. تا پیش از این هر فرم HTML خودش را
دستی می‌ساخت و نتیجه‌اش ناهماهنگی بود. حالا همه فرم‌ها از یک مجموعه
تابع مشترک ساخته می‌شوند:

- **جداکننده هزارگان زنده روی مبلغ‌ها** — هنگام تایپ خودکار گذاشته
  می‌شود و مکان‌نما سر جایش می‌ماند. ارقام فارسی هم پذیرفته می‌شود.
- **مبلغ به حروف** زیر هر فیلد مبلغ نوشته می‌شود («صد و بیست و هشت
  میلیون و چهارصد و پنجاه هزار ریال») تا اشتباه صفر دیده شود.
- **نشانه ★ روی فیلدهای اجباری** و برجسته‌شدن فیلد خالی هنگام ثبت،
  به‌جای پیام مبهم قبلی.
- **راهنمای کوتاه زیر هر فیلد** و **نمونه‌متن داخل فیلدها**.
- **بخش‌بندی فرم‌های بلند** با عنوان و آیکون.
- **میان‌بر تاریخ**: امروز / ۱ هفته / ۱ ماه / ۳ ماه، و درج خودکار «/».
- **کلید Enter** فرم را ثبت می‌کند.

فرم‌های بازنویسی‌شده: کالا، اشخاص، دریافت/پرداخت، چک، حساب بانکی،
گروه کالا. فرم فاکتور طبق نظر کاربر دست‌نخورده ماند.

### اصلاح برچسب‌های گمراه‌کننده
- فرم چک دو فیلد داشت که هر دو «بانک» نام داشتند — یکی بانکِ روی برگه
  و یکی حساب بانکی خودمان. حالا «بانک صادرکننده» و «حساب بانکی ما».
- «مرتبط» در فرم چک به «دریافت از» / «پرداخت به» تغییر کرد.
- «شید» → «شید رنگ»، «کالیته» → «کد کالیته»، «حداقل» → «حداقل موجودی»،
  «نام» → «نام کالا» / «نام گروه».

### موجودی هنگام انتخاب کالا در فاکتور
کنار نام هر کالا در فهرست انتخابِ ردیف فاکتور، موجودی فعلی نوشته
می‌شود («کتان کشی عرض ۱۵۰ (C-4120) │ موجودی: ۳۷ متر») یا «ناموجود».
این فقط برچسب نمایشی است — مقدار گزینه همان شناسه کالاست، پس هیچ چیزی
از این عدد وارد ردیف فاکتور یا دیتابیس نمی‌شود.

هنگام ویرایش یک فاکتور، اثر خودِ آن فاکتور از این عدد کنار گذاشته
می‌شود تا با کنترل «فروش بیش از موجودی» یکسان باشد. برای این کار
`Prod.stockMap` آرگومان اختیاری `excludeInvoiceId` گرفت.

### باگ‌های رفع‌شده
- **مانده شخص هنگام ویرایش صفر می‌شد.** فیلد «مانده اولیه» فقط در فرم
  شخص جدید معنی دارد، ولی `Con.save` بی‌قیدوشرط آن را می‌خواند؛ چون
  در حالت ویرایش فیلدی در صفحه نبود، مقدار صفر ذخیره می‌شد و مانده
  واقعی از بین می‌رفت. حالا فقط وقتی فیلد وجود دارد خوانده می‌شود.
- **ماژول جدید در حافظه آفلاین ثبت نشده بود** — تست خودکار گرفتش و
  `js/05b-form.js` به فهرست `sw.js` اضافه شد.

### سایر
- تست تازه `tests/formlive.js` با ۱۵ بررسی (جداکننده، حروف، ارقام
  فارسی، اعتبارسنجی، میان‌بر تاریخ، ذخیره درست مبلغ، موجودی در فاکتور،
  حفظ مانده هنگام ویرایش) به اجراکننده اضافه شد. مجموع: ۱۰ دسته.
- نسخه کش به `pb-v9.3` رسید.

## ۱. باگ‌هایی که باعث از کار افتادن برنامه می‌شدند

| # | محل | مشکل | اثر روی کار شما |
|---|-----|------|------------------|
| ۱ | `12-invoices.js` → `contactBal` | حلقه دریافت/پرداخت‌ها به آرایه `txs` که هرگز تعریف نشده بود مقدار اضافه می‌کرد | `ReferenceError`؛ **مانده حساب هر شخصی که حتی یک سند دریافت یا پرداخت داشت قابل محاسبه نبود** و فاکتورش هم چاپ نمی‌شد |
| ۲ | `21-pagination.js` | تابع `slice` مقادیر `total` و `pages` را در وضعیت نمی‌نوشت | `pages` برابر `NaN` می‌شد و **هیچ دکمه صفحه‌بندی نمایش داده نمی‌شد**؛ فقط ۲۰ رکورد اول دیده می‌شد |
| ۳ | `22-banks.js` | یک بلوک `bts.forEach` رهاشده به متغیرهای تعریف‌نشده `bankId` و `txs` ارجاع می‌داد | خطا در صفحه بانک‌ها |
| ۴ | `18-reports.js` → `_br` | متغیر `invs` دو بار با `var` تعریف شده بود | گزارش بدهکاران/بستانکاران |
| ۵ | `13-payments.js` | متغیر `tt` دو بار تعریف شده بود | برچسب «حواله» هرگز نمایش داده نمی‌شد |
| ۶ | `20-dashboard.js` | یک شیء `Bank` تکراری و مرده (~۲۸۵ خط) داخل داشبورد تعریف شده بود و نسخه اصلی را بازنویسی می‌کرد | رفتار غیرقابل پیش‌بینی در صفحه بانک‌ها |
| ۷ | `23-routes.js` | مسیرها `Promise` را برنمی‌گرداندند | صفحه‌ها روی چرخ‌دنده بارگذاری گیر می‌کردند و خطاهای داخل صفحه در هیچ‌جا گرفته نمی‌شد |
| ۸ | `18-reports.js` | نوشتن در `#rC` بدون بررسی وجود عنصر | اگر پیش از پایان بارگذاری گزارش صفحه را عوض می‌کردید، برنامه با `Cannot set properties of null` متوقف می‌شد |
| ۹ | `index.html` | آدرس CDN کتابخانه Chart.js ناقص بود | نمودارهای داشبورد بارگذاری نمی‌شدند |
| ۱۰ | `19-settings.js` | `DB.get('users', undefined)` | اگر نشست خراب بود، صفحه تنظیمات کامل از کار می‌افتاد |

## ۲. باگ‌های داده و محاسبات (خطر خرابی دفاتر)

### پشتیبان‌گیری و بازیابی — `08-backup.js` (بازنویسی شد)
- **سه جدول در پشتیبان نبود:** `banks`، `bankTransfers` و `users`. با بازیابی از پشتیبان،
  تمام حساب‌های بانکی و انتقالات بین‌بانکی شما **از بین می‌رفت**.
- **بازیابی، شناسه‌ها را دور می‌ریخت:** هنگام بازیابی فیلد `id` هر رکورد حذف می‌شد و
  IndexedDB شناسه جدید می‌داد. یعنی `contactId` روی فاکتورها، `productId` روی اقلام و
  `bankId` روی اسناد **به رکوردهای اشتباه اشاره می‌کردند** — کل ارتباط داده‌ها به هم می‌ریخت.
  اکنون شناسه‌ها عیناً حفظ می‌شوند و این با تست خودکار بررسی می‌شود.
- نوشتن رکوردها دسته‌ای شد (سرعت بازیابی فایل‌های بزرگ) و پیام‌های خطا گویا شدند.

### تقویم جلالی — `00-core.js`
- نسخه قبلی از فرمول ساده «سال میلادی + ۶۲۱» و ماه‌های ۳۰ روزه استفاده می‌کرد.
  جایش الگوریتم جدول breaks (همان الگوریتم `jalaali-js`) پیاده شد.
- **یک خطای یک‌روزه در تبدیل معکوس:** شرط کبیسه برای سال کاهش‌یافته سنجیده می‌شد نه سال
  اصلی. نتیجه این بود که تاریخ‌های دی، بهمن و اسفند بعضی سال‌ها **یک روز عقب‌تر** تبدیل
  می‌شدند (۳۹۵۶ روز از ۳۶۸۹۰ روز آزمون‌شده). اکنون هر دو جهت تبدیل برای تمام روزهای
  ۱۳۵۰ تا ۱۴۵۰ با تقویم رسمی مرورگر مطابقت کامل دارد.
- `getPeriodStart` در `02-utils.js` قبلاً ماه را ۳۰ روز ثابت فرض می‌کرد؛ حالا حساب واقعی
  تقویم انجام می‌شود، پس بازه گزارش‌های «ماه گذشته» درست است.

### از دست رفتن بی‌صدای داده — `03-db.js`
- هیچ‌یک از عملیات‌های نوشتن `onerror` و `onabort` نداشت. اگر ذخیره‌سازی مرورگر پر می‌شد یا
  تراکنش لغو می‌شد، **برنامه پیام موفقیت نشان می‌داد ولی داده ذخیره نشده بود**.
  اکنون همه از یک تابع مشترک `DB._req` می‌گذرند که خطا را بالا می‌دهد.

### محاسبه موجودی — `10-products.js` و `16-warehouse.js`
- صفحه انبار موجودی را فقط از فاکتورهای سال مالی جاری حساب می‌کرد، ولی کاردکس کالا از تمام
  سال‌ها. **دو صفحه دو عدد متفاوت نشان می‌دادند.** هر دو روی محاسبه تجمعی هم‌راستا شدند.
- تابع `stockMap` اضافه شد که موجودی همه کالاها را با یک بار پیمایش حساب می‌کند
  (قبلاً برای هر کالا تمام فاکتورها دوباره خوانده می‌شد — کند شدن شدید با داده زیاد).
- حذف کالایی که در فاکتوری استفاده شده، حالا جلوگیری می‌شود (قبلاً فاکتور به کالای
  ناموجود ارجاع می‌داد).

### پورسانت واسطه در فاکتور خرید — `12-invoices.js`
- فیلدهای واسطه و پورسانت فقط برای فاکتور فروش ذخیره می‌شدند. اگر روی فاکتور **خرید**
  واسطه و پورسانت وارد می‌کردید، **بی‌صدا دور ریخته می‌شد**. اکنون برای هر دو ذخیره می‌شود.

### حذف یک طرف حواله — `13-payments.js`
- حواله دو سند می‌سازد (یک دریافت و یک پرداخت) ولی حذف، فقط یکی را برمی‌داشت و حساب
  نامتوازن می‌ماند. اکنون با تأیید شما هر دو طرف حذف می‌شوند.

### جمع مانده حساب بانک — `22-banks.js`
- جمع در فهرست بانک‌ها و جمع در صفحه جزئیات هر بانک با دو فرمول متفاوت حساب می‌شدند و
  عدد یکسانی نمی‌دادند. هر دو به تابع مشترک `Bank.balance()` وصل شدند.

### خواندن اعداد فارسی — `00-core.js`
- تابع `numOf` جداکننده هزارگان فارسی (`٬`) را نمی‌شناخت، پس «۱۲٬۳۴۵» را **۱۲** می‌خواند.
  الان جداکننده‌های فارسی، عربی و لاتین و اعشار عربی (`٫`) درست پردازش می‌شوند.
- تمام ۲۳ فراخوانی `parseInt` بدون مبنا (radix) حذف و با `intOf` / `numOf` / `elNum`
  جایگزین شد. `parseInt` بدون مبنا روی ورودی مثل `08` می‌تواند نتیجه غیرمنتظره بدهد.

### خروجی CSV — `06-export.js`
- ستون‌های عددی با ارقام فارسی و جداکننده هزارگان صادر می‌شدند و **اکسل آن‌ها را متن
  می‌دید، نه عدد** — جمع زدن در اکسل کار نمی‌کرد. اکنون به عدد لاتین خام تبدیل می‌شوند.

### سال مالی — `07-fiscal-year.js`
- نام و تاریخ سال مالی پیش‌فرض از «سال میلادی + ۶۲۱» ساخته می‌شد که بین فروردین و دی
  یک سال خطا داشت. حالا از تقویم واقعی خوانده می‌شود و پایان سال با توجه به کبیسه بودن
  ۲۹ یا ۳۰ اسفند است.
- تاریخ شروع و پایان سال مالی حالا اعتبارسنجی می‌شوند (قبلاً هر رشته‌ای پذیرفته می‌شد و
  گزارش‌های دوره‌ای را خراب می‌کرد) و پایان نمی‌تواند قبل از شروع باشد.

## ۳. امنیت

- **رمز عبور:** `btoa` (کدگذاری برگشت‌پذیر، عملاً رمز آشکار) با **SHA-256** جایگزین شد.
  رمزهای قبلی همچنان کار می‌کنند و بار اول ورود، خودکار به هش امن ارتقا پیدا می‌کنند.
- **انقضای نشست:** ۱۲ ساعت. قبلاً نشست هرگز منقضی نمی‌شد.
- **بازنشانی رمز:** تابع `resetPass` بدون هیچ پرسشی همه کاربران را حذف می‌کرد؛
  الان تأیید صریح می‌گیرد.
- **حذف `eval`:** صفحه‌بندی از `eval` روی رشته استفاده می‌کرد؛ با ثبت تابع (`Pag.register`)
  جایگزین شد.
- **تزریق HTML:** هر مقداری که کاربر وارد می‌کند (نام کالا، نام شخص، شرح، شماره چک، …)
  اکنون از تابع `esc()` می‌گذرد — در سلول جدول‌ها، برچسب `<option>`، مقدار `value=""`،
  `textarea` و صفحه چاپ. قبلاً نام کالایی مثل `<img onerror=...>` به‌عنوان HTML اجرا می‌شد.
  این مورد با تست خودکار بررسی می‌شود.
- حداقل طول رمز جدید: ۶ کاراکتر.

## ۴. اعتبارسنجی ورودی‌ها

- **تاریخ:** همه‌جا با `Jalali.parse` اعتبارسنجی و نرمال‌سازی می‌شود. ورودی‌های
  `1404/1/5`، `۱۴۰۴-۰۱-۰۵` و `14040105` پذیرفته و به `1404/01/05` تبدیل می‌شوند؛
  تاریخ بی‌معنا (مثل `1404/13/01` یا `1404/12/30` که سال کبیسه نیست) رد می‌شود.
  قبلاً هر رشته‌ای ذخیره می‌شد و گزارش‌های بازه‌ای را بی‌صدا خراب می‌کرد.
- تخفیف بیشتر از جمع فاکتور، پرداختی بیشتر از مبلغ کل، و تعداد صفر یا منفی رد می‌شوند.
- فروش بیشتر از موجودی **هشدار** می‌دهد (مسدود نمی‌کند — بند ۴.۴ فایل README).
- مبلغ صفر یا منفی در سند دریافت/پرداخت و چک رد می‌شود.
- پیام‌های خطا از یک کلمه (مثل «مبلغ») به جمله راهنما تبدیل شدند
  (مثل «مبلغ باید بزرگ‌تر از صفر باشد»).
- تأییدیه‌های حذف مشخص می‌کنند چه چیزی حذف می‌شود.
- ساخت کاربر `admin` تکراری هنگام بازیابی پشتیبان جلوگیری می‌شود.

## ۵. بازسازی و پاک‌سازی

- تفکیک فایل تک‌پارچه ۱۴۳۳ خطی به `index.html` + `css/app.css` + ۲۵ ماژول جاوااسکریپت.
- **حذف ~۳۵۰ خط کد مرده:** شیء `Bank` تکراری در داشبورد، تابع `show` تکراری در بانک‌ها،
  بلوک‌های رهاشده.
- **حذف ~۶۰ خط HTML تکراری:** توابع «پیش‌نمایش فاکتور» و «چاپ فاکتور» تقریباً یکسان بودند
  و هر تغییری باید در دو جا اعمال می‌شد. الان یک سازنده مشترک `Inv._html(v, sz)` با
  پارامتر اندازه (A4/A5) دارند.
- توابع کمکی مشترک در `00-core.js`: `esc`، `toEnDigits`، `numOf`، `intOf`، `elVal`،
  `elNum`، `setHTML`، `el`، `todayJ` و شیء `Jalali`.
- صفحه‌بندی به فهرست چک‌ها و دریافت/پرداخت‌ها اضافه شد؛ ردیف جمع کل به پایین جدول‌ها
  اضافه شد.
- شناسه‌های تکراری عناصر رفع شد: `pBk` در فرم چک به `kBkAcc` و `trTo` در انتقال چک به
  `chkTrTo` تغییر کرد (با فرم دریافت/پرداخت تداخل داشتند).
- `manifest.json` و آیکون‌ها ساخته شدند (در `<head>` صدا زده می‌شد ولی وجود نداشت).
- خطاهای راه‌اندازی حالا در صفحه ورود نمایش داده می‌شوند و یک `unhandledrejection`
  سراسری هر خطای گرفته‌نشده را به کاربر اطلاع می‌دهد (قبلاً صفحه سفید می‌شد).
- کامنت فارسی بالای هر اصلاح گذاشته شده تا بعداً بدانید چرا آن‌طور نوشته شده است.


---

# نسخه ۹ — اصلاحات و قابلیت‌های جدید

## ۰. باگ بحرانی راه‌اندازی (برنامه بالا نمی‌آمد)

**خطای گزارش‌شده:** `elVal is not defined`

**علت:** فایل `js/00-core.js` — که همه توابع پایه (`elVal`، `esc`، `numOf`، `intOf`،
`elNum`، `setHTML`، `el`، `todayJ` و شیء `Jalali`) در آن است — ساخته شده بود ولی
هیچ‌وقت در تگ‌های `<script>` فایل `index.html` صدا زده نشده بود. پس همه آن توابع
در مرورگر وجود نداشتند و برنامه در اولین فراخوانی متوقف می‌شد.

**اصلاح:** `00-core.js` به‌عنوان اولین اسکریپت به `index.html` اضافه شد.

**جلوگیری از تکرار:** تست‌ها قبلاً فایل‌ها را با `readdirSync` از پوشه `js/` می‌خواندند،
یعنی فایلی را که در `index.html` جا افتاده بود هم اجرا می‌کردند و باگ را نمی‌دیدند.
حالا تست ترتیب واقعی `<script src>` را از `index.html` بیرون می‌کشد و دو چیز را
تضمین می‌کند: (۱) هر فایل موجود روی دیسک در HTML صدا زده شده باشد، (۲) هیچ فایلِ
صدازده‌شده‌ای گم نشده باشد.

## ۱. سند دریافت/پرداخت خودکار از فاکتور (بند ۴.۱ — گزینه الف)

- فیلد «پرداخت شده» فاکتور حالا خودش یک سند دریافت/پرداخت می‌سازد:
  فاکتور فروش → سند **دریافت**، فاکتور خرید → سند **پرداخت**.
- فاکتور یک فیلد جدید **«حساب دریافت/پرداخت فاکتور»** دارد. اگر بانکی انتخاب شود،
  مبلغ در گردش همان حساب بانکی ثبت و در گزارش آن حساب دیده می‌شود.
- سند خودکار به فاکتور گره خورده است (`sourceInvoiceId`): با ویرایش مبلغ فاکتور
  به‌روز می‌شود، با صفر شدن مبلغ حذف می‌شود، با حذف فاکتور هم حذف می‌شود.
- این اسناد در فهرست دریافت/پرداخت با برچسب **«خودکار»** نشان داده می‌شوند و
  ویرایش یا حذف مستقیم آن‌ها بسته است — چون منبع حقیقت، خودِ فاکتور است.
- **جلوگیری از دوباره‌شماری:** حالا که سند مستقل وجود دارد، مبلغ «پرداخت شده»
  از سه جای دیگر که قبلاً جداگانه حساب می‌شد برداشته شد: `Inv.contactBal`،
  `Bank.balance` و ردیف تکراری در صورتحساب (`15-ledger.js`).

## ۲. علامت مانده بر اساس عرف حسابداری (بند ۴.۲)

**تغییر در منطق مالی — لطفاً بخوانید.**

قرارداد جدید: **مثبت = بدهکار** (طرف حساب به شما بدهکار است)،
**منفی = بستانکار** (شما به او بدهکار هستید). این همان عرف رایج حسابداری است.

قبلاً مانده اولیه در `getOpenBal` قرینه می‌شد، یعنی عدد مثبتِ واردشده در فرم
به بستانکار تبدیل می‌شد و نتیجه برعکس عرف بود.

**مهاجرت خودکار داده:** در اولین اجرای نسخه ۹، علامت `balance` همه طرف‌حساب‌های
موجود یک بار برگردانده می‌شود تا معنای عددها با قرارداد جدید بخواند. مقدار اصلی
در فیلد `balanceLegacy` نگه داشته می‌شود. **قبل از اولین اجرا یک پشتیبان بگیرید.**

## ۳. موجودی اولیه انبار (بند ۴.۳)

طبق نظر شما تغییری داده نشد؛ موجودی انبار همان‌طور از فاکتورها محاسبه می‌شود.

## ۴. مسدودسازی کامل فروش بیشتر از موجودی (بند ۴.۴)

- ثبت فاکتور فروش با کمبود موجودی حالا **کاملاً رد می‌شود** (قبلاً فقط هشدار بود).
- پیام خطا برای هر کالا موجودی فعلی و مقدار درخواستی را نشان می‌دهد و می‌گوید
  «ابتدا فاکتور خرید را ثبت کنید».
- در ویرایش فاکتور، مقدار قبلی همان فاکتور از محاسبه کنار گذاشته می‌شود تا
  ویرایش یک فاکتور موجود بی‌دلیل رد نشود.

## ۵. بستن سال مالی با انتقال مانده‌ها

- گزینه «بستن سال» سال مقصد را می‌پرسد و بعد:
  مانده پایان دوره هر طرف حساب را به‌عنوان **مانده اولیه سال بعد** ثبت می‌کند،
  مانده حساب‌های بانکی را هم به سال بعد منتقل می‌کند.
- سال بسته‌شده **فقط‌خواندنی** می‌شود: ثبت و ویرایش فاکتور، دریافت/پرداخت و چک
  در آن سال بسته است (`FY.assertOpen`).
- بازکردن سال بسته‌شده فقط برای نقش «مدیر» ممکن است.

## ۶. چند کاربر با سطح دسترسی

چهار نقش: **مدیر** (دسترسی کامل)، **حسابدار** (بدون مدیریت کاربران)،
**کاربر ثبت** (بدون حذف و بدون بستن سال)، **فقط مشاهده**.

- صفحه «کاربران و سطوح دسترسی» برای ساخت، ویرایش و غیرفعال‌کردن کاربر.
- آخرین مدیر فعال قابل حذف یا تنزل نیست (قفل‌شدن بیرون از برنامه).
- کاربر غیرفعال اجازه ورود ندارد؛ منو و دکمه‌ها بر اساس نقش پنهان/بسته می‌شوند.
- **صادقانه:** تا وقتی داده در مرورگر است این محدودیت‌ها فقط رابط کاربری‌اند و
  امنیت واقعی نیستند. امنیت حقیقی بعد از انتقال به Supabase و فعال‌کردن
  Row Level Security به دست می‌آید (فایل `supabase/schema.sql`).

## ۷. جست‌وجوی سراسری

- یک کادر جست‌وجو روی همه چیز: طرف‌حساب‌ها، کالاها، فاکتورها (شامل نام کالاهای
  داخل فاکتور)، دریافت/پرداخت‌ها، چک‌ها و حساب‌های بانکی.
- ارقام فارسی/عربی، «ی/ک» عربی و نیم‌فاصله یکسان‌سازی می‌شوند، پس «۱۰۰۱» و
  «1001» یک نتیجه می‌دهند.
- کلید میان‌بر **Ctrl+K** (یا Cmd+K)، بسته‌شدن با Escape یا کلیک بیرون.
- کلیک روی نتیجه مستقیم به صورتحساب، کارت کالا یا فاکتور می‌رود؛ اگر فاکتور در
  سال مالی دیگری باشد تذکر داده می‌شود.

## ۸. کارکرد کامل بدون اینترنت (Service Worker)

- فایل `sw.js` اضافه شد: فایل‌های برنامه با راهبرد
  stale-while-revalidate و منابع CDN با راهبرد cache-first نگه داشته می‌شوند.
- فایل‌ها تک‌تک کش می‌شوند تا یک خطای ۴۰۴ کل نصب را خراب نکند؛ کش نسخه‌های
  قدیمی هنگام فعال‌شدن پاک می‌شود.
- فقط روی http/https ثبت می‌شود (با باز کردن مستقیم فایل کار نمی‌کند).

## ۹. دکمه بازگشت مرورگر

مسیرها با `history.replaceState` ثبت می‌شدند، یعنی هیچ سابقه‌ای ساخته نمی‌شد و
دکمه «بازگشت» کاربر را از برنامه بیرون می‌انداخت. حالا `pushState` استفاده می‌شود
و شنونده‌های `popstate`/`hashchange` اضافه شدند، پس بازگشت و جلو درست کار می‌کند.

## ۱۰. مسیر انتقال به سرور (Vercel + Supabase)

برای اینکه بعداً از چند دستگاه به آخرین اطلاعات وصل شوید، این نسخه از حالا آماده شد:

- **`supabase/schema.sql`** — ساختار کامل جدول‌ها برای Postgres:
  کلید اصلی `uuid`، جداسازی کسب‌وکار با `org_id` و جدول `memberships`،
  اقلام فاکتور به‌صورت جدول مستقل `invoice_items` (نه JSON)، مبالغ با
  `numeric(18,2)` به‌جای عدد اعشاری شکننده، ایندکس یکتا که تضمین می‌کند هر فاکتور
  فقط یک سند خودکار داشته باشد، تریگر `updated_at`، سیاست‌های Row Level Security
  بر اساس نقش، و تریگری که ثبت در سال مالی بسته را جلو می‌گیرد.
- **`js/03c-sync.js`** — صف خروجی (outbox): هر افزودن/ویرایش/حذف در یک صف داخلی
  هم ثبت می‌شود، هر رکورد `uid` یکتا و `updatedAt` دارد و هر دستگاه شناسه خودش را
  دارد. **این ماژول فعلاً هیچ چیزی به هیچ سروری نمی‌فرستد** — فقط زیرساخت را آماده
  نگه می‌دارد تا وقت انتقال، سابقه تغییرات از دست نرفته باشد.
- در صفحه «تنظیمات» بخش «وضعیت برنامه» تعداد تغییرات در انتظار همگام‌سازی،
  شناسه دستگاه و نسخه ساختار داده را نشان می‌دهد، و دکمه «خروجی آماده بارگذاری
  روی سرور» یک JSON با ساختار جدول‌های مقصد می‌سازد.

## ۱۱. اعتبارسنجی

۱۷۲ تست خودکار (jsdom + fake-indexeddb) روی این نسخه اجرا و همه قبول شد:
یکپارچگی بارگذاری فایل‌ها، مهاجرت داده و تکرارپذیر بودنش، قرارداد علامت مانده،
ساخت/به‌روزرسانی/حذف سند خودکار و نبودِ دوباره‌شماری، انتقال مانده در بستن سال،
فقط‌خواندنی بودن سال بسته، اختیارات هر چهار نقش، جست‌وجو (شامل تست تزریق HTML)،
صف همگام‌سازی، و رندر شدن هر ۱۷ مسیر برنامه. برنامه هم در مرورگر واقعی
(Chromium) اجرا و همه صفحه‌ها بدون خطای زمان اجرا باز شدند.


---

# نسخه ۹.۱ — رفع گیر کردن روی صفحه لودینگ

## علت (ریشه‌ای)

برنامه روی صفحه لودینگ می‌ماند، **بدون هیچ پیام خطایی**، و فرم ورود هم نمی‌آمد.

در `<head>` فایل `index.html` دو شیت استایل از CDN بارگذاری می‌شد:

- فونت وزیرمتن از `fonts.googleapis.com`
- آیکون‌ها از `cdnjs.cloudflare.com`

شیت استایل **رندر را بلاک می‌کند** و مرورگر تا رسیدن پاسخ آن، اجرای
`<script>`های بعدی را هم متوقف می‌کند. یعنی اگر آن دو آدرس *کند یا بی‌جواب*
باشند (نه اینکه خطا بدهند — بی‌جواب بمانند)، هیچ‌کدام از ۲۹ اسکریپت برنامه
اجرا نمی‌شود. حتی `elVal` هم تعریف نمی‌شود، `try/catch` راه‌اندازی هرگز اجرا
نمی‌شود، پس هیچ خطایی هم نمایش داده نمی‌شود.

این حالت در مرورگر واقعی بازتولید شد: با بی‌جواب گذاشتن درخواست‌های CDN،
دقیقاً همان صحنه تکرار شد — صفحه لودینگ تا بی‌نهایت، `elVal` تعریف‌نشده.

برای برنامه‌ای که قرار است **بدون اینترنت** کار کند این ایراد بنیادی بود:
عملاً بدون اینترنت بالا نمی‌آمد.

## اصلاح

### ۱. حذف کامل وابستگی به CDN

فونت، آیکون‌ها و Chart.js داخل پروژه آمدند (پوشه `vendor/`، حدود ۷۰۰ کیلوبایت):

```
vendor/
├── vazirmatn.css              فونت وزیرمتن، ۶ وزن
├── bootstrap-icons.css        آیکون‌ها
├── chart.umd.min.js           نمودارها
└── fonts/                     فایل‌های woff2
```

حالا `index.html` هیچ آدرس اینترنتی ندارد. نتیجه آزمون: با قطع کامل اینترنت
برنامه بالا می‌آید، فونت فارسی درست است، آیکون‌ها می‌آیند و نمودار داشبورد
هم رسم می‌شود — چیزهایی که قبلاً بدون اینترنت کار نمی‌کردند.

### ۲. نگهبان راه‌اندازی (تا دیگر هیچ‌وقت بی‌صدا گیر نکند)

یک اسکریپت کوچک **داخل خود `index.html`** و پیش از هر فایل دیگری اجرا می‌شود.
اگر ۸ ثانیه بگذرد و برنامه آماده نشده باشد، روی صفحه لودینگ یک جدول عیب‌یابی
نشان می‌دهد: زمان سپری‌شده، اینکه توابع پایه و لایه پایگاه داده بارگذاری شده‌اند
یا نه، تعداد اسکریپت‌ها، وضعیت اینترنت، و **فهرست دقیق فایل‌هایی که بارگذاری
نشدند**. آزمایش شد: با حذف `js/24-boot.js` پنل ظاهر شد و نام همان فایل را نوشت.

### ۳. رفع دو راه گیر کردن دیگر در پایگاه داده

- **`onblocked` تعریف نشده بود:** اگر برنامه در تب دیگری باز باشد، ارتقای نسخه
  پایگاه داده بلاک می‌شود و راه‌اندازی برای همیشه معطل می‌ماند. حالا پیام روشن
  می‌دهد: «برنامه در یک تب دیگر باز است…».
- **`onversionchange` اضافه شد:** این اتصال با درخواست ارتقا از تب دیگر بسته
  می‌شود تا آن تب بلاک نشود.
- **زمان‌سنج ۱۵ ثانیه‌ای:** اگر پایگاه داده به هر دلیلی جواب نداد، به‌جای
  انتظار بی‌پایان خطا می‌دهد.

### ۴. نام‌گذاری مراحل راه‌اندازی

هر مرحله (بازکردن پایگاه داده، ساخت کاربر پیش‌فرض، مهاجرت داده، ورود،
بارگذاری صفحه) نام و سقف زمانی دارد. اگر خطایی رخ دهد، پیام می‌گوید **کدام
مرحله** بود: «خطا در راه‌اندازی (مرحله: مهاجرت داده) — …».

### ۵. Service Worker

نسخه کش به `pb-v9.1` تغییر کرد و فایل‌های `vendor/` هم کش می‌شوند.


---

# نسخه ۹.۲ — سند وصول چک، خروجی گزارش‌ها، تست‌های داخل پروژه

## تصمیم درباره سند حسابداری دوطرفه

بررسی شد و **اضافه نشد**. سند دوطرفه (کدینگ حساب، دفتر روزنامه، تراز
آزمایشی) برای وقتی لازم است که حسابرس یا اظهارنامه مالیاتی در کار باشد؛
در استفاده شخصی فقط هر فاکتور را مجبور می‌کند دو بار ثبت شود. مانده‌ها در
وضعیت فعلی درست محاسبه می‌شوند و همان کافی است.

## ۱. سند خودکار هنگام وصول چک

**مشکل:** با وصول چک فقط وضعیتش عوض می‌شد. هیچ سندی در صفحه دریافت/پرداخت
ساخته نمی‌شد — برخلاف فاکتور که سند خودکار می‌سازد. یعنی پولی وارد بانک
می‌شد که هیچ سندی نداشت.

**اصلاح:** `Chk.syncAutoPayment` اضافه شد، دقیقاً هم‌الگوی
`Inv.syncAutoPayment`. فقط چکِ «وصول‌شده»ای که حساب بانکی دارد سند می‌سازد،
چون تنها در آن لحظه پول واقعاً وارد یا خارج بانک می‌شود. با تغییر وضعیت به
هر چیز دیگر (مثلاً برگشتی) سند خودکار حذف می‌گردد. حذف یا ویرایش چک هم سند
را همگام می‌کند تا سند یتیم نماند.

سند با برچسب «خودکار» نمایش داده می‌شود و دستی قابل ویرایش یا حذف نیست؛
برای تغییر باید خود چک اصلاح شود.

**نکته مهم درباره مانده اشخاص:** این سند روی مانده شخص اثر ندارد. مانده
شخص از خودِ چک محاسبه می‌شود، یعنی **به‌محض دریافت چک** بدهی طرف کم
می‌شود، نه هنگام وصول. این عمداً حفظ شد چون همان رفتار قبلی و درست است.
اگر سند هم شمرده می‌شد، بدهی طرف دو برابر کم می‌شد؛ برای همین در محاسبه
مانده و دفتر معین، سندهای دارای `sourceCheckId` رد می‌شوند.

## ۲. دو باگ در دفتر بانک

هنگام کار روی مورد بالا پیدا شد. **هیچ‌کدام روی عدد «مانده بانک» اثر
نداشت، ولی صفحه گردش حساب بانک را اشتباه نشان می‌داد:**

- **دوباره‌شماری فاکتور:** صفحه گردش حساب، مبلغ «پرداخت شده» فاکتور را
  مستقیم به دفتر اضافه می‌کرد، در حالی که همان فاکتور یک سند دریافت واقعی
  هم می‌سازد که جداگانه شمرده می‌شد. نتیجه: هر پرداخت فاکتور **دو ردیف**
  در دفتر بانک می‌ساخت. این دوباره‌شماری قبلاً از `Bank.balance` حذف شده
  بود ولی از این صفحه نه.
- **جابه‌جایی بدهکار و بستانکار:** مانده با فرمول `مانده + بدهکار −
  بستانکار` حساب می‌شود، ولی ردیف‌های دریافت/پرداخت و چک وارونه ثبت
  می‌شدند. یعنی هر «دریافت» مانده دفتر را **کم** می‌کرد.

روی هم، این دو باگ همدیگر را خنثی می‌کردند و مانده پایانی دفتر صفر
درمی‌آمد در حالی که مانده واقعی بانک ۱۰٬۰۰۰٬۰۰۰ بود. هر دو اصلاح شد و
حالا دفتر بانک با مانده بانک می‌خواند.

## ۳. خروجی Excel و PDF روی هر ۷ گزارش

قبلاً فقط دفتر معین خروجی داشت. حالا هر هفت گزارش (خلاصه، خرید، فروش،
موجودی، سود و زیان، بدهکاران، بستانکاران) دکمه Excel و PDF دارند.

به‌جای نوشتن هفت تابع جداگانه که با هر تغییر گزارش از کار می‌افتاد، جدولِ
همان لحظه از روی صفحه خوانده می‌شود؛ پس هر تغییری در گزارش‌ها خودبه‌خود در
خروجی هم اعمال می‌گردد. گزارش‌هایی که کارت خلاصه دارند به شکل
«عنوان / مقدار» صادر می‌شوند. ارقام فارسی در فایل CSV به عدد لاتین تبدیل
می‌شوند تا اکسل آن‌ها را عدد بشناسد نه متن.

## ۴. تست‌ها داخل پروژه آمدند

پوشه `tests/` اضافه شد با ۹ دسته تست و یک اجراکننده:

```bash
node tests/run-all.js          تست‌های منطقی (سریع، بدون مرورگر)
node tests/run-all.js --all    به‌علاوه تست‌های مرورگری
```

تست‌های مرورگری جدید: باز شدن هر ۱۶ صفحه و ۷ گزارش، چرخه کامل چک (دریافت،
وصول، برگشت، حذف)، خروجی گزارش‌ها، و بالا آمدن با قطع کامل اینترنت.
راهنمای کامل در `tests/README.md`.

## ۵. دکمه نصب روی دستگاه

برنامه از قبل شرایط نصب را داشت (manifest، Service Worker، آیکون) ولی
دکمه‌ای نداشت و کاربر باید از منوی مرورگر پیدایش می‌کرد. حالا کنار دکمه
حالت شب، دکمه نصب می‌آید — البته فقط روی HTTPS یا localhost و فقط وقتی
برنامه هنوز نصب نشده باشد.

## ۶. ممیزی کامل صفحه‌ها

هر ۱۶ صفحه و هر ۷ تب گزارش در مرورگر واقعی باز و خطاهایشان ضبط شد:
**صفر خطا**. این آزمون حالا به‌صورت `tests/audit.js` در پروژه ماندگار است.
